# Installation guide

    $ git clone https://github.com/mikugami/Plane-task.git
    $ cd Plane-task
    $ mkdir build
    $ cd build
    $ cmake ..
    $ make

Run 
    $ ./main 
for fancy debugging triangle
    $ ./main -planescene
for main program
